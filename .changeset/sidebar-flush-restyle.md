---
'@mastra/playground-ui': patch
---

Restyled `MainSidebar` with updated icons and cleaner navigation visuals.

- Improved section-title and nav-item readability across themes.
- Nav items render flush with section titles, and extra divider and active-edge treatments were removed for a cleaner layout.
- Added `WorkspacesIcon`, `RequestContextIcon`, `ScorersIcon`, `DatasetsIcon`, `ExperimentsIcon`, and `MetricsIcon`; updated `AgentIcon`, `PromptIcon`, `WorkflowIcon`, `ProcessorIcon`, `McpServerIcon`, `ToolsIcon`, `LogsIcon`, and `TraceIcon`.
- The legacy `indent` option is still accepted but no longer changes layout.
