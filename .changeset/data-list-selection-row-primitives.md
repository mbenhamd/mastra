---
'@mastra/playground-ui': patch
---

Added selection-aware, condensed `DataList` row primitives for building list views that match the Traces and Logs visual style, including reusable ID, mono text, date, time, and select cells plus row layout props for leading selection controls.
