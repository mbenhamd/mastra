---
'@mastra/core': patch
---

Fixed cancellation overwriting newer or completed workflow runs when storage supports conditional updates. Fixed stale workflow completion events and return values after a rejected terminal update.
