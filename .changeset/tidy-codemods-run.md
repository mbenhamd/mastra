---
'@mastra/codemod': patch
---

Run the declared jscodeshift executable with discrete process arguments. Paths containing spaces now work, verbose mode uses the supported level syntax, and custom runner options can be passed safely by repeating `--jscodeshift`.
