---
'mastra': patch
---

Fix CLI output alignment: streamed build logs, studio deploy SSE logs, server deploy polled logs, and `deploy logs` output now display with the clack pipe prefix (`│`) for visual continuity under their parent step.
