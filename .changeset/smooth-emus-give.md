---
'@mastra/server': patch
---

Fixed generated Server declarations and embedded schema declarations when a module and a directory share a name. Preserved module formats in embedded voice declarations. Validated the API schema manifest against its declared response schema while preserving the emitted route contracts.
