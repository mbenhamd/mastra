---
'@mastra/factory': patch
---

Fixed Factory Slack channel configuration failing type checks when modern tool display options coexist with legacy renderers. Modern display settings take precedence, while legacy-only configuration retains its renderer options.
