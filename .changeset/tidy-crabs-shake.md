---
'@mastra/core': patch
---

Restored the `mastra_thread_state` storage constants (`TABLE_THREAD_STATE`, `THREAD_STATE_SCHEMA`, schema and table-config registrations) that store implementations already import.
