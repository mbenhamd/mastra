---
'@internal/playground': patch
---

Fixed the test-chat composer on the agent editor page rejecting typed input. Typing in the composer textarea while editing an agent's system prompt, tools, or variables now works the same as it does on the regular chat page.
