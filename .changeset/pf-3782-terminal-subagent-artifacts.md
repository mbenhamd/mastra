---
'@mastra/core': patch
---

Preserved bounded, JSON-safe artifact metadata from a verified child terminal tool result when `spawn_subagent` uses final delivery. The direct answer keeps its caller-facing text while generated artifacts remain available to UI transports, and oversized or unverified metadata is omitted.
