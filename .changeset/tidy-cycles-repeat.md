---
'@mastra/tanstack-start': minor
---

Added @mastra/tanstack-start server adapter for mounting Mastra in TanStack Start apps via a catch-all server route. Uses the same Hono-based MastraServer pattern as @mastra/next.
